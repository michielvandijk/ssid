# ipf_seed

ipf_seed

## Usage

``` r
ipf_seed(
  reg_tz,
  hh_s,
  per_s,
  bm,
  p,
  reg_sample = FALSE,
  verbose = FALSE,
  max_iter = 300,
  max_ratio = 5,
  min_ratio = 0.2,
  relative_gap = 0.01,
  absolute_diff = 10
)
```

## Arguments

- absolute_diff:
