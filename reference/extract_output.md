# Extract weights and diagnostics from spatial simulation object

Extract weights and diagnostics from spatial simulation object

## Usage

``` r
extract_output(df, var)
```

## Arguments

- df:

  Object with spatial simulation results nested by ssp-year and region.

- var:

  Type of output that needs to be extracted. Options are: stats,
  weight_tbl, primary_comp, secondary_comp
